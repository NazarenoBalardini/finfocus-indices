name: Actualizar Pisos Mínimos

on:
  schedule:
    - cron: '0 4 * * *'  # Todos los días a las 04:00 UTC
  workflow_dispatch:

jobs:
  actualizar-pisos:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Instalar dependencias (si existen)
        run: |
          if [ -f requirements.txt ]; then
            echo "Encontrado requirements.txt, instalando..."
            pip install --upgrade pip
            pip install -r requirements.txt
          else
            echo "No hay requirements.txt, saltando instalación."
          fi

      - name: Ejecutar script de actualización
        run: |
          if [ -f scripts/actualizar_pisos.py ]; then
            python scripts/actualizar_pisos.py
          elif [ -f actualizar_pisos.py ]; then
            python actualizar_pisos.py
          else
            echo "Error: no se encontró actualizar_pisos.py en ninguna ruta esperada."
            exit 1
          fi
